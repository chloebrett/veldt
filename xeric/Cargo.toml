[package]
name = "xeric"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
hyper = { version = "0.14", features = ["full"] }
tonic = "0.9.2"
tokio = { version = "1.43.0", features = ["rt-multi-thread"] }
