[package]
name = "xeric"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
hyper = { version = "0.14", features = ["full"] }
tokio = { version = "1", features = ["rt-multi-thread"] }
tonic = "0.9.2"
