[package]
name = "mesic"
version = "0.1.0"
edition = "2024"

[dependencies]
dasp_graph = { version = "0.11.0", features = ["node-boxed"] }
dasp_ring_buffer = "0.11.0"
ordered-float = "5.0.0"
petgraph = "0.5" # Needs to match version used by dasp_graph.
ringbuffer = "0.15.0"
shared = { path = "../shared" }
