syntax = "proto3";

package render;

import "pmodel.proto";

service Render {
  rpc Render (RenderRequest) returns (RenderReply) {}
}

message RenderRequest {
  pmodel.ProjectProto project = 1;
}

message RenderReply {
  bytes audio = 1;
}
